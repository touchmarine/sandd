http:
  routers:
    to-codesearch:
      rule: "Host(`cs.sd.test`)"
      service: codesearch
      # route tls and ignore non-tls requests
      tls: {}

  services:
    codesearch:
      loadBalancer:
        servers:
          - url: "http://host.docker.internal:2473"
